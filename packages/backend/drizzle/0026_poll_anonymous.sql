-- Анонимные и публичные опросы (#17).
--
-- По умолчанию TRUE (анонимный): это менее раскрывающий вариант, и если кто-то забудет выбрать,
-- лучше не показать лишнего, чем показать. Обратной дороги у раскрытия нет.
ALTER TABLE polls ADD COLUMN IF NOT EXISTS anonymous boolean NOT NULL DEFAULT true;
