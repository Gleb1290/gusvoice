-- Своя цена у каждого звука саундборда (запрос 02.09: «звуки могут быть не одной цены,
-- потому что будут ультимативные звуки и звуки так себе»).
--
-- 🔴 **В МИНУТАХ сидения, как и весь остальной прайс.** В монеты переводит сервер по текущей ставке.
-- Храни мы цену в монетах — один сдвиг ползунка ставки разом переоценил бы весь набор звуков, и
-- «ультимативный» стал бы дешевле «так себе» просто потому, что их заводили в разные дни.
--
-- ⚠️ NULL — это НЕ ноль, а «цены своей нет, действует общая из каталога». Так уже залитые звуки не
-- осиротели при этой миграции, а свежий turnkey-сервер работает без единой настройки.
ALTER TABLE server_soundboard ADD COLUMN IF NOT EXISTS price_minutes integer;
