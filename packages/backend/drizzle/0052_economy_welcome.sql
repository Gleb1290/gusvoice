-- Приветственное окно экономики: показано ли человеку (запрос 03.09).
--
-- 🔴 Отметка на СЕРВЕРЕ, а не в localStorage: у человека десктоп, браузер и телефон, и с каждого
-- окно выскакивало бы заново. Один раз — значит один раз на человека.
-- ⚠️ Момент, а не флаг: «когда показали» бесплатно даёт ответ на «а он вообще видел презентацию до
-- того, как спросил, откуда монеты».
ALTER TABLE users ADD COLUMN IF NOT EXISTS economy_welcome_seen_at timestamptz;
