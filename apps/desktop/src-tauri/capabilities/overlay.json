{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "In-game overlay windows: the voice roster (\"overlay\") and the transient tip/pinch toast (\"toast\"). Both receive state pushes from the main window (core events) and can be dragged to reposition (start-dragging). Desktop-only — neither window is created on mobile.",
  "windows": ["overlay", "toast"],
  "platforms": ["windows", "linux", "macOS"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
